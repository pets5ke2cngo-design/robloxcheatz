import React from 'react';
import ReactDOM from 'react-dom/client';
import DowngradeApp from './DowngradeApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DowngradeApp />
  </React.StrictMode>
);
